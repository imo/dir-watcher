cn = require 'coffee-new'

task 'autotest', 'watch & test when changes are detected', (options) ->
	cn.autoTest()

task 'build', 'compile src/ to lib/', (options) ->
	cn.build()

task 'test', 'test', (options) ->
	cn.run 'expresso'